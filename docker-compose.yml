version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: pertemuan11_db
      POSTGRES_USER: pertemuan_user
      POSTGRES_PASSWORD: abcde
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
